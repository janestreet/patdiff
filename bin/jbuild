(executables
 ((names (patdiff))
  (review_help true)
  (libraries (core_extended
              patdiff_lib
              pcre
              sexplib))))

(install
 ((section bin)
  (files ((patdiff.exe as patdiff)
          patdiff-git-wrapper))))

(jbuild_version jane_street)
