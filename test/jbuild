
(library
 ((name patdiff_test)
(flags (:standard -safe-string))
  (libraries (
    async
    core
    expect_test_helpers
    ))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version 1)
